scenarios:
  - id: test-reconnect-scenario
    goal: "Test scenario with device reconnect"
    initializationMethods:
      - type: Reconnect
      - type: Wait
        durationMs: 3000  # Wait after reconnect if needed
      - type: LaunchApp
        packageName: com.example.app
    maxStep: 5
    maxRetry: 2

settings:
  prompt:
    additionalSystemPrompts: []